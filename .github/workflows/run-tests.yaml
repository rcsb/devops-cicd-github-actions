name: Run tests

on:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ["self-hosted","java"]
    steps:
      - uses: actions/checkout@v3
      - run: |
          java --version
          mvn --version
      - run: mvn test --no-transfer-progress